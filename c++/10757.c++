#include <iostream>
#include <string.h>
using namespace std;

int main() {
  char a[10005] = {0, }, b[10005] = {0, };
  int sum[10010] = {0, };
  int a_len, b_len;
  cin>>a>>b;
  a_len = strlen(a);
  b_len = strlen(b);
  for(int i=1; ; i++) {

  }
}