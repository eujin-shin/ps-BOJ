#include <stdio.h>

int n;
int col[15] = {0, };
int dia_1[15] = {0, };
int dia_2[15] = {0, };

void queen(int row) {
  for(int i=0; i<n; i++) {
    if(!col[n] && !dia_1[row+i] && !dia_2[row-i+n-1]) {
      
    }
  }
}

int main() {

}